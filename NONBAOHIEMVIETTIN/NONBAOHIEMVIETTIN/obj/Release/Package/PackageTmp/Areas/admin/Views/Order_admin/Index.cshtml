@using NONBAOHIEMVIETTIN.Models;
@model PagedList.IPagedList<order>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Danh sách đơn hàng";
    Layout = "~/Areas/admin/Views/Shared/_Layout.cshtml";
    var db = new nonbaohiemviettinEntities();
}
@section styles{
    <style>
        .img-product {
            width: 100px;
            height: 100px;
        }

        .modal-body {
            overflow-y: scroll;
            max-height: 70vh;
        }

            .modal-body p {
                margin: 0;
            }
    </style>
}
@section scripts{
    @if (TempData["status"] != null)
    {
        <script>
            $(function () {
                alertify.success(`@TempData["status"]`);
            })
        </script>
    }
}
<!-- DataTales Example -->
<div class="card shadow mb-4">
    <div class="card-header py-3 d-flex align-items-center justify-content-between flex-wrap">
        <h6 class="m-0 font-weight-bold text-primary">Danh sách đơn hàng</h6>
        <form method="get" action="/tim-kiem-don-hang" class="d-sm-inline-block form-inline ml-md-3 my-2 my-md-0 mw-100 navbar-search">
            <div class="input-group">
                <input name="tukhoa" type="text" class="form-control bg-light border-0 small" placeholder="Tìm kiếm..." aria-label="Search" aria-describedby="basic-addon2">
                <div class="input-group-append">
                    <button class="btn btn-primary" type="submit">
                        <i class="fas fa-search fa-sm"></i>
                    </button>
                </div>
            </div>
        </form>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="ui-sortable-table table table-bordered" id="dataTable" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Tên tài khoản</th>
                        <th>Ngày đặt</th>
                        <th>Họ tên</th>
                        <th>Trạng thái</th>
                        <th>Đã chuyển tiền?</th>
                        <th>Tổng giá</th>
                        <td>Công cụ</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr id="_order_@item.id">
                            <td>@item.id</td>
                            <td>@item.accounts.username</td>
                            <td>@DateTime.Parse(item.createdate.ToString()).ToShortDateString()</td>
                            <td>@item.fullname</td>
                            <td>@(item.status == false ? Html.Raw("<span id='confirm_order_" + @item.id + "' class='text-danger'>Chưa duyệt (<a data-id='" + item.id + "' class='confirm_order' style='cursor:pointer;'>Click vào đây</a> để duyệt)</span>") : Html.Raw("<span class='text-success'>Đã duyệt</span>"))</td>
                            <td>@(item.statuspay == false ? Html.Raw("<span id='transfer_order_" + @item.id + "' class='text-danger'>Chưa chuyển tiền (<a data-id='" + item.id + "' class='transfer_order' style='cursor:pointer;'>Click vào đây</a> để xác nhận chuyển tiền)</span>") : Html.Raw("<span class='text-success'>Đã chuyển tiền</span>"))</td>
                            <td>@HoTro.Instances.convertVND(item.total.ToString())</td>
                            <td>
                                <div class="tool d-flex align-items-center flex-column justify-content-center">
                                    <a data-toggle="modal" data-target="#order_@item.id" title="Xem đơn hàng mã @item.id" class="d-sm-inline-block btn btn-sm btn-primary shadow-sm">
                                        <i class="far fa-eye"></i>
                                    </a>
                                    <a data-id="@item.id" title="Xoá đơn hàng mã @item.id" class="mt-1 mb-1 delete_order d-sm-inline-block btn btn-sm btn-primary shadow-sm">
                                        <i class="far fa-trash-alt"></i>
                                    </a>
                                    <a data-toggle="modal" data-target="#order_@item.id-detail" title="Xem chi tiết đơn hàng mã @item.id" class="d-sm-inline-block btn btn-sm btn-primary shadow-sm">
                                        <i class="fas fa-info-circle"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                        <!-- Order Modal-->
                        <div class="modal fade" id="order_@item.id" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
                            <div class="modal-dialog" role="document">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h5 class="modal-title" id="exampleModalLabel">Thông tin đơn hàng có mã <strong>@item.id</strong></h5>
                                        <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                                            <span aria-hidden="true">×</span>
                                        </button>
                                    </div>
                                    <div class="modal-body">
                                        <p><strong>Mã:</strong> @item.id</p>
                                        <p><strong>Tên tài khoản:</strong> @item.accounts.username</p>
                                        <p><strong>Ngày đặt: </strong> @DateTime.Parse(item.createdate.ToString()).ToShortDateString()</p>
                                        <p><strong>Email:</strong> <a href="mailto:@item.email">@item.email</a></p>
                                        <p><strong>Họ tên khách hàng:</strong> @item.fullname</p>
                                        <p><strong>Địa chỉ:</strong> @item.address</p>
                                        <p><strong>Điện thoại:</strong><a href="tel:@item.phone">@item.phone</a></p>
                                        <p><strong>Trạng thái:</strong> @(item.status == false ? Html.Raw("<span id='confirm_order_detail_" + @item.id + "' class='text-danger'>Chưa duyệt (<a data-id='" + item.id + "' class='confirm_order' style='cursor:pointer;'>Click vào đây</a> để duyệt)</span>") : Html.Raw("<span class='text-success'>Đã duyệt</span>"))</p>
                                        <p><strong>Đã chuyển tiền?</strong> @(item.statuspay == false ? Html.Raw("<span id='transfer_order_detail_" + @item.id + "' class='text-danger'>Chưa chuyển tiền (<a data-id='" + item.id + "' class='transfer_order' style='cursor:pointer;'>Click vào đây</a> để xác nhận chuyển tiền)</span>") : Html.Raw("<span class='text-success'>Đã chuyển tiền</span>"))</p>
                                        <p><strong>Tổng giá:</strong> @HoTro.Instances.convertVND(item.total.ToString())</p>
                                        <p><strong>Ghi chú:</strong> @item.note</p>                                       
                                    </div>
                                    <div class="modal-footer">
                                        <button class="btn btn-secondary" type="button" data-dismiss="modal">Đóng</button>
                                    </div>
                                </div>
                            </div>
                        </div>

                       

                    }
                </tbody>
            </table>
        </div>
    </div>

</div>
@foreach (var item in Model)
{
    <div class="modal fade" id="order_@item.id-detail" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Thông tin đơn hàng có mã <strong>@item.id</strong></h5>
                    <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">×</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div class="card shadow mb-4">
                        <div class="table-responsive">
                            <table class="ui-sortable-table table table-bordered" cellspacing="0">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Tên sản phẩm</th>
                                        <th>Giá</th>
                                        <th>Số lượng</th>
                                        <th>Thành tiền</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item_detail in db.orderdetail.Where(x => x.idorder == item.id).ToList())
                                    {
                                        <tr>
                                            <td>@item_detail.idorder</td>
                                            <td>@item_detail.products.name</td>
                                            <td>@HoTro.Instances.convertVND(item_detail.price.ToString())</td>
                                            <td>@item_detail.quantity</td>
                                            <td>@HoTro.Instances.convertVND(item_detail.subtotal.ToString())</td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-secondary" type="button" data-dismiss="modal">Đóng</button>
                </div>
            </div>
        </div>
    </div>

}
@if (ViewBag.last != 1)
{
    <ul class="pagination mr-auto ml-auto" style="justify-content:center;">
        @if (ViewBag.check == true)
        {
            @Html.PagedListPager(Model,
        page => Url.Action("Index", new { page }),
        new PagedListRenderOptions
        {
            MaximumPageNumbersToDisplay = 3
        })
        }
        else
        {
            @Html.PagedListPager(Model, page => Url.Action("Search", new { tukhoa = Request.Url.Query.Substring(Request.Url.Query.IndexOf("?tukhoa="), Request.Url.Query.LastIndexOf('&') == -1 ? Request.Url.Query.Length : Request.Url.Query.LastIndexOf('&')).Replace("?tukhoa=", ""), page }),
                                    new PagedListRenderOptions
                                    {
                                        MaximumPageNumbersToDisplay = 5
                                    });
        }

    </ul>
}